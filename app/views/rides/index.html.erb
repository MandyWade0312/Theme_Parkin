<p id="notice"><%= notice %></p>

<h1>Rides Available to be Reviewed</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rides.each do |ride| %>
      <tr>
        <td><%= ride.name %></td>
        <td><%= link_to 'Show', ride %></td>
    <% end %>
  </tbody>
</table>

<br>


<%
=begin%>
 <%= link_to 'New Ride', new_ride_path %> 
<%
=end%>
