<%= form_with(scope: :session, url: login_path, local: true) do |form| %>
  <div class="danger">
    <%= flash[:danger] %>
  </div>

<h1 class="mx-6 my-2 text-2xl font-bold text-blue-900">Log In</h1>

  <div class="m-6">
    <%= form.label :email, class: "font-bold text-blue-900" %> 
    <%= form.email_field :email, class: "border-2 border-purple-300 focus:outline-none focus:ring-2 focus:ring-purple-600 focus:border-transparent" %> 

    <%= form.label :password, class: "font-bold text-blue-900" %>
    <%= form.password_field :password, class: "border-2 border-purple-300 focus:outline-none focus:ring-2 focus:ring-purple-600 focus:border-transparent" %>
  </div>

  <%= form.submit "Log in", class: "m-6 bg-blue-700 hover:bg-blue-500 text-green-100 font-bold py-2 px-4 border border-green-700 rounded" %>
<% end %>
